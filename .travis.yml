sudo: required

services:
  - docker

before_install:
  - docker build -t dadadev/pro-dev-flow-implementation -f Dockerfile.dev .

script:
  - docker run dadadev/pro-dev-flow-implementation -e CI=true npm run test -- --coverage